import { colors, palette } from "./colors";
import { layout, responsiveSpacing, screenSize, spacing } from "./spacing";
import { radius } from "./radius";
import { customFontFamilies, fontFamilies, fontSizes, fontWeights, lineHeights, typography } from "./typography";
import { shadows } from "./shadows";

export {
  colors,
  customFontFamilies,
  fontFamilies,
  fontSizes,
  fontWeights,
  layout,
  lineHeights,
  palette,
  radius,
  responsiveSpacing,
  screenSize,
  shadows,
  spacing,
  typography,
};

export const themeTokens = {
  colors,
  customFontFamilies,
  fontFamilies,
  fontSizes,
  fontWeights,
  layout,
  lineHeights,
  palette,
  radius,
  responsiveSpacing,
  screenSize,
  shadows,
  spacing,
  typography,
};

export default themeTokens;
